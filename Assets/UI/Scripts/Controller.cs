using UnityEngine;

namespace UI.Main
{
    public class Main : MonoBehaviour
    {
        // Start is called before the first frame update
        
        // Control the transition between UI screens
        
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
